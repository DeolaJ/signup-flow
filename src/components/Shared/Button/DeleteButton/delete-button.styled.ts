import styled from 'styled-components';
import BaseButton from '../base';

export const DeleteButtonContainer = styled(BaseButton)`
  color: var(--color-white);
  background: var(--color-red);
  border: 1px solid var(--color-red);
`;
